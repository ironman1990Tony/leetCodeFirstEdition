#include <iostream>
#include <assert.h>

using namespace std;
class Solution {
public:
    bool isMatch(string s, string p) {
       // assert(s == ""  )
    }
};
int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
